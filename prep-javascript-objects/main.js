const person = {
  firstName: 'Julie',
  lastName: 'Dumbrique',
  hobby: 'playing cozy games on my switch'
};

console.log(person);

const fullName = person.firstName + ' ' + person.lastName;
console.log(fullName);

person.job = 'learning to code.';
console.log(person.job);

person.previousjob = 'quality associate.';
console.log(person.previousjob);

console.log(person);
